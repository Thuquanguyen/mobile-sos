import 'package:flutter/material.dart';

class ListEvent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Events Empty!")),);
  }
}
